1. Install NodeJS
2. Install grunt-cli globally with npm install -g grunt-cli
3. Install bower globally with npm install -g bower
4. In the js app folder cd .\PISS.Web\App   Install the npm packages with npm install   Install the bower components with bower install
5. Build the angular-bootstrap   cd .\lib\angular-bootstrap   npm install   grunt before-test   grunt after-test   cd ..\..\
6. Build the js app with grunt